# Terraform — never commit state, lock files, provider binaries, or user-specific values.
**/.terraform/
**/.terraform.lock.hcl
**/.terraform.tfstate.lock.info
**/terraform.tfstate
**/terraform.tfstate.backup
**/terraform.tfvars

# Workshop env files generated by setup scripts (contain secrets / personal config)
.ate-dev-env.sh
.are-keycloak-env

# Editor / OS noise
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Helm + Kubernetes temp
**/values.local.yaml
*.tgz

# Python
__pycache__/
*.pyc
.venv/
venv/

# Personal Terraform module - keep local, do not commit
agentregistry-enterprise/ar-gke/


*agentregistry-enterprise/ar-gke*